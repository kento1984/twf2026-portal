.claude/
# Playwright MCP screenshots (local debugging only)
.playwright-mcp/

# Phase 6: pamphlet page renders は Cloudflare Pages へ同梱するため、
# Phase 7 step-6 で prototype/data/pamphlet_pages/ に移動済み。git 同梱する。
# (旧: data/pamphlet_pages/ を ignore していたが、新場所では同梱したいので除外しない)

# Local prototype screenshots (regenerable via scripts/screenshot_top.py)
prototype/screenshots/

# Phase 6 step 2 / Kangxi正規化: backup files written by excel_mapper.py / normalize_kangxi.py
# (.bak は git履歴で代替できるため追跡不要)
*.bak

# Phase 7 step 5/6: prototype/attachments/ は Cloudflare Pages 配信用に同梱するが、
# sync_attachments.py が許可した拡張子 (pdf/docx/xlsx/pptx) 以外は誤同梱を防ぐため除外。
prototype/attachments/**/*.txt
prototype/attachments/**/*.zip
prototype/attachments/**/image[0-9][0-9][0-9].gif
prototype/attachments/**/image[0-9][0-9][0-9].png
prototype/attachments/**/image[0-9][0-9][0-9].jpg
prototype/attachments/**/image[0-9][0-9][0-9].jpeg
prototype/attachments/**/image[0-9][0-9][0-9].bmp

# scripts/_inspect_excel.py 実行で出る一時ファイル
inspect_output.txt

# Phase 7 step-8: PDF→PNG レンダリング (vision agent への入力、再生成可、78MB)
data/_pdf_pages/

# 機密: OpenAI 等の API キー
.env
.env.*
!.env.example

scripts/_*.py
!scripts/_extract_koike_slide_data.py

# 検証スクリプトのワンショットレポート (再生成可、追跡不要)
docs/_*.md

# Phase 1.0: Playwright 検証スクショ (毎回上書き再生成、追跡不要)
verify-*.png

# Phase 1.0: 前方針の残骸スクショ (二度と使わない)
topics-banner-*.png

# Phase 1.0: シルエット画像のリビジョン履歴 (柏原ローカル保管、commit対象外)
prototype/assets/top-pickup-3spots-pc.v*.png

# メーカー提供PDF等の一時作業領域 (画像変換元、git追跡不要)
tmp/
